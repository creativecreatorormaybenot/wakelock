name: wakelock_platform_interface
description: A common platform interface for the wakelock plugin used by the different platform implementations.
version: 0.1.0
homepage: https://github.com/creativecreatorormaybenot/wakelock/tree/master/wakelock_paltform_interface

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic: ^1.9.2
